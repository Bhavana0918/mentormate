apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: webapp-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi  # Requested storage size, should match with PersistentVolume
  storageClassName: manual  # Use the same storage class as defined in PV
